pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url 'https://repo.maven.apache.org/maven2' }
    }
}

enableFeaturePreview('TYPESAFE_PROJECT_ACCESSORS')

rootProject.name = 'BigLittleFixes'

rootProject.children.each { project ->
    // keep default behavior; individual modules will be added later if needed
}
